{
  "name": "cloudflare-r2-explorer",
  "version": "1.0.0",
  "description": "A Cloudflare R2 bucket explorer with a smooth and user-friendly interface.",
  "main": "app.js",
  "scripts": {
    "build": "echo 'Building the project...' && mkdir -p dist && cp -r * dist --exclude dist",
    "deploy": "gh-pages -d dist"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "gh-pages": "^3.2.3"
  }
}
